services:
  static-api:
    build: .
    ports:
      - '3000:3000'
    environment:
      STATIC_DIR: /app/datos
      HTTP_PORT: '3000'
      DYNAMIC_CACHE_TTL_MS: '900000'
      POCKETBASE_URL: ${POCKETBASE_URL:-https://db.argentinadatos.com}
      POCKETBASE_TOKEN: ${POCKETBASE_TOKEN:-}
    volumes:
      # Coolify Compose: mounts the repo checkout's datos/ (refreshed on each deploy).
      - ./datos:/app/datos:ro
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "fetch('http://127.0.0.1:3000/health').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
